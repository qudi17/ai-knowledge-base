# 通用 RAG + Agent 架构设计

> 从多个优秀项目中提炼的通用方案

**版本**：0.1.0  
**状态**：设计草稿  
**最后更新**：2026-02-28

---

## 🎯 设计目标

### 核心原则

1. **极简主义** - 核心代码 <2,000 行
2. **高度可插拔** - 所有组件可替换
3. **场景适配** - 支持 80% 常见场景
4. **生产就绪** - 安全、监控、可运维

### 不适用场景

- ❌ 超大规模（亿级文档）
- ❌ 实时性要求极高（<100ms）
- ❌ 特殊领域（医疗、法律等需要定制）

---

## 🏗️ 通用架构

```
┌─────────────────────────────────────────────────────────┐
│                    应用层（业务系统）                      │
├─────────────────────────────────────────────────────────┤
│                  Universal Agent SDK                     │
│  ┌───────────────────────────────────────────────────┐  │
│  │              Agent Core（核心引擎）                 │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌───────────┐ │  │
│  │  │ 消息处理循环 │  │ 上下文管理  │  │ 会话管理  │ │  │
│  │  └─────────────┘  └─────────────┘  └───────────┘ │  │
│  └───────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────┐  │
│  │              RAG Pipeline（检索增强）               │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌───────────┐ │  │
│  │  │ 文档处理    │  │ 向量化      │  │ 检索/重排 │ │  │
│  │  └─────────────┘  └─────────────┘  └───────────┘ │  │
│  └───────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────┐  │
│  │              Tool System（工具系统）                │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌───────────┐ │  │
│  │  │ 工具注册表   │  │ 内置工具    │  │ 自定义工具 │ │  │
│  │  └─────────────┘  └─────────────┘  └───────────┘ │  │
│  └───────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────┐  │
│  │              Memory System（记忆系统）              │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌───────────┐ │  │
│  │  │ 短期记忆    │  │ 长期记忆    │  │ 记忆压缩  │ │  │
│  │  └─────────────┘  └─────────────┘  └───────────┘ │  │
│  └───────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────┐  │
│  │              Provider Layer（模型适配）             │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌───────────┐ │  │
│  │  │ LLM Provider│  │ Embedding   │  │ Rerank    │ │  │
│  │  └─────────────┘  └─────────────┘  └───────────┘ │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 📦 核心模块设计

### 1. Agent Core（~500 行）

**职责**：消息处理、工具调度、流程控制

**核心接口**：
```python
class Agent:
    async def process_message(self, user_input: str, context: dict) -> str
    async def run(self) -> None
```

**关键设计**：
- ✅ 异步消息循环
- ✅ 工具调用最大迭代限制
- ✅ 进度反馈机制
- ✅ 错误处理与重试

**可替换组件**：
- [ ] 消息循环策略（ReAct / Plan-and-Execute）
- [ ] 上下文构建策略
- [ ] 会话管理策略

---

### 2. RAG Pipeline（~800 行）

**职责**：文档处理、检索、上下文增强

**核心流程**：
```
文档 → 分块 → 向量化 → 存储 → 检索 → 重排序 → 上下文
```

**核心接口**：
```python
class RAGPipeline:
    async def ingest(self, documents: List[Document]) -> None
    async def retrieve(self, query: str, top_k: int) -> List[Document]
    async def query(self, query: str, context: dict) -> str
```

**可替换组件**：
- [ ] 分块策略（固定大小 / 语义分块 / 递归分块）
- [ ] 向量化模型（OpenAI / 本地 / 多模型）
- [ ] 向量数据库（Chroma / Pinecone / Weaviate / Memory）
- [ ] 检索策略（密度 / 稀疏 / 混合）
- [ ] 重排序模型（Cohere / BGE / 无）

---

### 3. Tool System（~400 行）

**职责**：工具注册、验证、执行

**核心接口**：
```python
class Tool(ABC):
    @property
    def name(self) -> str: ...
    
    @property
    def description(self) -> str: ...
    
    @property
    def parameters(self) -> dict: ...
    
    async def execute(self, **kwargs) -> str: ...

class ToolRegistry:
    def register(self, tool: Tool) -> None
    async def execute(self, name: str, params: dict) -> str
```

**内置工具**：
- ✅ 文件操作（read/write/edit/list）
- ✅ Shell 命令（exec）
- ✅ Web 工具（search/fetch）
- ✅ API 调用（http_request）
- ✅ 子代理（spawn）

**可替换组件**：
- [ ] 工具验证策略
- [ ] 执行沙箱
- [ ] 结果格式化

---

### 4. Memory System（~300 行）

**职责**：短期记忆、长期记忆、记忆压缩

**双层设计**：
```
短期记忆（Session） → 最近 N 轮对话
长期记忆（MEMORY.md） → 结构化事实
历史日志（HISTORY.md） → 时间线索引
```

**核心接口**：
```python
class MemoryStore:
    def get_context(self) -> str
    def save(self, key: str, value: str) -> None
    def search(self, query: str) -> List[str]
    async def consolidate(self, messages: List) -> None
```

**可替换组件**：
- [ ] 记忆压缩策略（LLM 总结 / 提取事实）
- [ ] 存储后端（文件 / 数据库 / 向量）
- [ ] 检索策略（关键词 / 向量）

---

### 5. Provider Layer（~300 行）

**职责**：LLM、Embedding、Rerank 模型适配

**核心接口**：
```python
class LLMProvider(ABC):
    async def chat(self, messages, tools, model) -> LLMResponse

class EmbeddingProvider(ABC):
    async def embed(self, texts: List[str]) -> List[List[float]]

class RerankProvider(ABC):
    async def rerank(self, query: str, documents: List) -> List
```

**支持模型**：
- LLM：Claude / GPT / Qwen / DeepSeek / 本地
- Embedding：OpenAI / BGE / M3E / 本地
- Rerank：Cohere / BGE-Reranker / 无

**可替换组件**：
- [ ] 模型路由策略
- [ ] 降级策略
- [ ] 缓存策略

---

## 🔧 配置系统

### 分层配置

```python
@dataclass
class Config:
    # Agent 配置
    agent: AgentConfig = field(default_factory=AgentConfig)
    
    # RAG 配置
    rag: RAGConfig = field(default_factory=RAGConfig)
    
    # 记忆配置
    memory: MemoryConfig = field(default_factory=MemoryConfig)
    
    # 模型配置
    models: ModelConfig = field(default_factory=ModelConfig)
    
    # 安全配置
    security: SecurityConfig = field(default_factory=SecurityConfig)
```

### 配置方式

- [ ] 代码配置（优先）
- [ ] YAML/JSON 文件
- [ ] 环境变量
- [ ] 配置中心（生产）

---

## 🛡️ 安全设计

### 三层防护

```
┌─────────────────────────────────────┐
│  应用层：用户认证、权限控制           │
├─────────────────────────────────────┤
│  Agent 层：输入过滤、输出审查         │
├─────────────────────────────────────┤
│  执行层：Sandbox、命令白名单、超时   │
└─────────────────────────────────────┘
```

### 核心机制

1. **输入过滤**
   - Prompt 注入检测
   - 敏感词过滤
   - 长度限制

2. **执行安全**
   - Workspace 限制
   - 命令白名单/黑名单
   - 资源配额

3. **输出审查**
   - 敏感信息过滤
   - 格式验证
   - 长度限制

---

## 📊 性能优化

### 缓存策略

```
┌─────────────────────────────────────┐
│  L1: Redis 缓存（完全相同查询）       │
├─────────────────────────────────────┤
│  L2: 语义缓存（相似问题）             │
├─────────────────────────────────────┤
│  L3: Prompt 缓存（相同上下文）        │
├─────────────────────────────────────┤
│  L4: 向量缓存（相同文档）             │
└─────────────────────────────────────┘
```

### 异步优化

- ✅ 全异步 IO
- ✅ 并发工具调用
- ✅ 流式响应
- ✅ 后台任务（记忆压缩）

### 批量优化

- [ ] 批量向量化
- [ ] 批量检索
- [ ] 批量工具调用

---

## 🎯 场景适配

### 场景 1：企业知识库

**配置**：
```python
config = Config(
    rag=RAGConfig(
        chunk_size=500,
        chunk_overlap=50,
        embedding_model="bge-m3",
        vector_store="chroma",
        retrieval_strategy="hybrid",
        rerank_model="bge-reranker"
    ),
    agent=AgentConfig(
        max_iterations=5,
        tools=["read_file", "search_knowledge_base"]
    )
)
```

### 场景 2：个人助手

**配置**：
```python
config = Config(
    memory=MemoryConfig(
        strategy="two_layer",
        consolidation_window=20
    ),
    agent=AgentConfig(
        max_iterations=10,
        tools=["read_file", "write_file", "exec", "web_search"]
    )
)
```

### 场景 3：客服系统

**配置**：
```python
config = Config(
    rag=RAGConfig(
        chunk_size=300,
        embedding_model="text-embedding-3-large",
        retrieval_strategy="hybrid",
        rerank_model="cohere"
    ),
    security=SecurityConfig(
        output_filter=True,
        sensitive_words=True
    )
)
```

---

## 📋 实施路线图

### Phase 1：核心提炼（2 周）
- [x] nanobot 分析
- [ ] langchain 分析
- [ ] llama_index 分析
- [ ] dify 分析
- [ ] 提炼通用 Agent Core
- [ ] 提炼通用 RAG Pipeline

### Phase 2：组件开发（3 周）
- [ ] 实现通用 Agent Core
- [ ] 实现通用 RAG Pipeline
- [ ] 实现通用 Tool System
- [ ] 实现通用 Memory System
- [ ] 实现 Provider Layer

### Phase 3：场景验证（2 周）
- [ ] 企业知识库场景
- [ ] 个人助手场景
- [ ] 客服系统场景
- [ ] 性能基准测试

### Phase 4：生产就绪（2 周）
- [ ] 监控指标
- [ ] 日志系统
- [ ] 配置管理
- [ ] 运维文档

---

## 📚 参考项目

| 项目 | 分析文档 | 提炼组件 |
|------|---------|---------|
| nanobot | ✅ 已完成 | Agent Core, Tool System |
| langchain | 待分析 | RAG Pipeline, Tool System |
| llama_index | 待分析 | RAG Pipeline, Index Strategy |
| dify | 待分析 | Workflow, UI |
| openclaw | 待分析 | Memory System, Session |

---

## 🔗 相关资源

- [项目分析模板](./项目分析模板.md)
- [nanobot 研究](../nanobot-research/)
- [universal_agent](../universal_agent/)

---

**维护者**：Eddy  
**状态**：设计草稿  
**下次更新**：完成 langchain 分析后
